<Window x:Class="AutoModel.GenerateFile"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AutoModel"
        mc:Ignorable="d"
        Title="生成Models类库" Height="150" Width="360" WindowStartupLocation="CenterOwner" ShowInTaskbar="False" WindowStyle="ToolWindow">
    <DockPanel Margin="20">
        <DockPanel DockPanel.Dock="Top" VerticalAlignment="Top" Margin="0,10,0,0">
            <Label Content="保存位置：" Width="70" />
            <Button x:Name="but_browes" DockPanel.Dock="Right" Content="浏览..." Margin="5,0,0,0" Padding="8, 2" Click="but_browes_Click" />
            <TextBox x:Name="text_path" IsReadOnly="True" VerticalContentAlignment="Center" />
        </DockPanel>
        <DockPanel VerticalAlignment="Bottom" Height="25">
            <Button x:Name="but_ok" Content="确定" Padding="8, 2" DockPanel.Dock="Right" Margin="5,0,0,0" Click="but_ok_Click" />
            <Button x:Name="but_cancel" Content="取消" Padding="8, 2" DockPanel.Dock="Right" HorizontalAlignment="Right" Height="25" VerticalAlignment="Top" Click="but_cancel_Click" />
        </DockPanel>
    </DockPanel>
</Window>
